#contributor : Satoshi Nishimura <nishim314@gmail.com>
#name : pub ...(...)
#group : definitions
# --
/**
 * ${1:function_title}
 *
 * ${2:description}
 * 
 * @author ${3:`(save-excursion
 (beginning-of-buffer)
 (if (re-search-forward "@author \\\\(.*\\\\)$" nil t)
     (setq match-string
       (buffer-substring-no-properties
         (match-beginning 1) (match-end 1))) "author <email>"))`}
 */
public function ${4:name}($5)
{
   $0
}
